This page has acces to the following variables:
<ul>
  <li>@current_user</li>
  <li>@user</li>
  <li>@roll_with_frames</li>
  <li>@frames</li>
</ul>

If you were to get more frames from these rolls, use:
Shelby::API.Shelby::API.get_roll_with_frames(@user['personal_roll_id'], skip=20, limit=20)

<br>

<ul>
  <% @frames.each do |f| %>
    <li>
      <% if f and f['video'] %>
        <img src="<%=f['video']["thumbnail_url"] %>" />
      <% end %>
    </li>
  <% end %>
</ul>
