<div class="shelf__wrapper">
  <div class="shelf">
    <% (@days + 1).times do |d| %>
      <ul class="list one-third">
        <% @videos[d].each do |video| %>
          <li class="list__item">
            <%= render :partial => 'objects/zeddmore_item',
               :locals => {
                :video => video,
                :frame => video #just tricking frame['id'] to not fail
               }
             %>
             <script class="js-video-json"><%= raw video.to_json %></script>
          </li>
        <% end %>
      </ul>
    <% end %>
  </div><!-- eo shelf -->
</div><!-- eo shelf__wrapper -->
