<% if @frame and @video %>
	<%= @video['title'] %> on shelby.tv
	
<% elsif @roll %>
	The <%= @roll['title'].titlecase %> Roll, curated <%= "#{@user ? "by #{@user['name']}" : ""}" %> on Shelby.tv
	
<% else %>
	<%= Settings::Application.title %>
	
<% end %>