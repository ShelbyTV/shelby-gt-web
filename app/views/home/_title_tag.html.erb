<% if @frame and @video %>
	<%= @video['title'] %> on shelby.tv
	
<% elsif @roll %>
	<%= @roll['subdomain'] %>.shelby.tv, a curated roll by <%= "#{@user ? "by #{@user['name']}" : ""}" %> on Shelby.tv
	
<% else %>
	<%= Settings::Application.title %>
	
<% end %>