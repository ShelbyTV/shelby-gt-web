class GeniusController < ApplicationController

  # Nothing here for now, but likely will need some logic soon, so still setting
  # up as controller + views instead of true non-Rails static content
  
end
