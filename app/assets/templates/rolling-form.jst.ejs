<% 
	var messageText = "↬ @"+frame.get('creator').get('nickname');
  var messages = (frame.get('conversation') && frame.get('conversation').get('messages')) || [];
  if(messages.length > 0){
		messageText += ": "+messages.first().get('text');
	}
	
  var active_networks = [];
  user.get('authentications').forEach(function(auth){
    active_networks.push(auth.provider);
  });

  var pathForDisplay = rollOptions.pathForDisplay;
  var titleWithoutPath = rollOptions.titleWithoutPath;
  var urlForRoll = rollOptions.urlForRoll;

  var userAvatar = userAvatar = user.get('user_image_original') || user.get('user_image');
  var userNickname = user.get('nickname') || "";
%>
<form class="new-comment clearfix">
  <div class="media_module">
    <a href="#" onclick="return false;" class="media_image">
      <span class="image_module xuser-avatar<%= (!userAvatar) ? ' missing-avatar' : ''%>" style="background-image: url(<%= userAvatar %>);" role="img">
        <img src="<%= userAvatar %>" class="visuallyhidden" title="<%= user.get('nickname') %>" alt="<%= user.get('nickname') %>">
      </span>
    </a>
    <div class="media_body">
      <textarea id="js-rolling-message" class="js-add-message-input new-comment-input" rows="2" autofocus tabindex="0"><%= messageText %></textarea>
      <div class="js-frame-comment-error-message frame-comment-error-message"></div>
      <!-- <button id="roll-it" class="new-comment-submit js-new-comment-submit js-track-event" data-ga_category="Conversation" data-ga_action="Click submit comment" data-ga_label="<%= user.get('nickname') %>" tabindex="0">Comment</button> -->
			<button id="js-roll-it" class="roll-options-submit" type="submit">Roll It!</button>
    </div>
  </div><!-- eo .media_module-->
</form>
