<h1 class="dynamic_video_info__h2">Like what you're watching?</h1>
<button class="button_primer button_default button_command button_command--like js-like-frame <%= currentVideoIsInQueue ? ' queued' : '' %> js-track-event"
      data-ga_category="<%= eventTrackingCategory %>"
      data-ga_action="Click like on current frame"
      data-ga_label="<%= shelby.models.user.get('nickname') %>">
  <i class="icon icon-heart <%= currentVideoIsInQueue ? 'icon-heart--red ': ''%>js-command-icon"></i>
  <span class="label">
    <%= (currentVideoIsInQueue) ? 'Liked' : 'Like' %>
  </span>
</button>
<h2 class="dynamic_video_info__h3">
  <% if (user.isAnonymous()) { %>
    All your likes are saved for you when you sign up!
  <% } else { %>
    <b>Protip:</b> View all your liked videos in the <a onclick="return false;" href="#" class="js-likes-section">Likes</a> section!
  <% } %>
</h2>
