development:
  adapter: redis
  connection: redis://localhost:6379/0
  # collecting off by default so that development environments aren't required to have redis installed
  collecting: true

staging:
  collecting: false

production:
  adapter: redis
  connection: redis://108.166.56.26:6379/0
  collecting: true
